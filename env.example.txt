# Discord Bot Token 設定
SHEN_ZE_TOKEN=
GU_BEICHEN_TOKEN=
FAN_CHENGXI_TOKEN=

# Google Generative AI 設定
GOOGLE_API_KEY=

# Firebase 設定
FIREBASE_CREDENTIALS_JSON={"type": "service_account","project_id": ... ,"universe_domain": "googleapis.com"}

# ===== 權限設定方式 =====

# 方式1: 全域權限設定（所有 Bot 使用相同權限）
ALLOWED_GUILDS=
ALLOWED_CHANNELS=

# 方式2: 個別 Bot 權限設定（每個 Bot 不同權限）
# 如果設定了個別權限，會優先使用個別設定，否則使用全域設定

# 沈澤的專屬權限
SHEN_ZE_ALLOWED_GUILDS=
SHEN_ZE_ALLOWED_CHANNELS=

# 顧北辰的專屬權限
GU_BEICHEN_ALLOWED_GUILDS=
GU_BEICHEN_ALLOWED_CHANNELS=

# 范丞㰕的專屬權限
FAN_CHENGXI_ALLOWED_GUILDS=
FAN_CHENGXI_ALLOWED_CHANNELS=

# 注意：
# - 如果某個 Bot 沒有設定個別權限，會自動使用全域設定
# - 個別權限的格式：{角色ID大寫}_{權限類型}
# - 角色ID會自動轉換為大寫（shen_ze -> SHEN_ZE） 